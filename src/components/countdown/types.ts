export type SlotDraft = {
  title: string;
  start_date: string;
  target_date: string;
};

export type ProgressStats = {
  progress: number;
  totalDays: number;
  doneDays: number;
  remainDays: number;
};
